# Component Inventory - Frontend

> 自動生成於 2026-01-18 | MRBS-frontend Vue.js 元件清單

## 概覽

| 屬性 | 值 |
|------|---|
| **框架** | Vue.js 3.4 |
| **元件總數** | 20+ |
| **路由數** | 11 |
| **外部庫** | FullCalendar, SweetAlert2, DOMPurify, Axios |

---

## 元件分類

### 📄 頁面元件 (Views)

| 元件 | 路徑 | 說明 |
|------|------|------|
| `homeView.vue` | `/views/` | 首頁視圖 |
| `aboutView.vue` | `/views/` | 關於頁面 |

### 🧩 功能元件 (Components)

#### 核心功能

| 元件 | 檔案 | 說明 | 認證 |
|------|------|------|------|
| `lobby.vue` | `/components/` | 使用者大廳 - 預約主介面 | ✅ requiresAuth |
| `calendar.vue` | `/components/` | FullCalendar 日曆元件 | - |
| `application.vue` | `/components/` | 預約申請表單 | - |
| `conference.vue` | `/components/` | 會議管理（管理員） | ✅ requiresManagerAuth |
| `reservation.vue` | `/components/` | 預約 CRUD 操作 | - |

#### 看板系統

| 元件 | 檔案 | 說明 | 認證 |
|------|------|------|------|
| `board.vue` | `/components/` | 看板管理 | ✅ requiresManagerAuth |
| `preview.vue` | `/components/` | 看板預覽 | ✅ requiresManagerAuth |
| `demo.vue` | `/components/` | 看板展示（公開） | ❌ |

#### 規則與文件

| 元件 | 檔案 | 說明 | 認證 |
|------|------|------|------|
| `rule.vue` | `/components/` | 使用規則編輯器（Quill） | ✅ requiresManagerAuth |
| `ruleDemo.vue` | `/components/` | 規則展示（公開） | ❌ |

#### 管理功能

| 元件 | 檔案 | 說明 | 認證 |
|------|------|------|------|
| `privilege.vue` | `/components/` | 權限管理 | ✅ requiresManagerAuth |
| `log.vue` | `/components/` | 操作日誌查看 | ✅ requiresManagerAuth |

#### 導航與認證

| 元件 | 檔案 | 說明 |
|------|------|------|
| `header.vue` | `/components/` | 頁面標頭 |
| `rootMenu.vue` | `/components/` | 管理員側邊選單 |
| `welcome.vue` | `/components/` | 歡迎頁面 |
| `failedLogin.vue` | `/components/` | 登入失敗頁面 |
| `helloWorld.vue` | `/components/` | 範例元件 |

### 🔒 中介層 (Middleware)

| 元件 | 檔案 | 說明 |
|------|------|------|
| `auth.vue` | `/middleware/` | 認證檢查，驗證 JWT 及權限 |

### 🎨 圖示元件 (Icons)

| 元件 | 檔案 |
|------|------|
| `IconCommunity.vue` | `/components/icons/` |
| `IconDocumentation.vue` | `/components/icons/` |
| `IconEcosystem.vue` | `/components/icons/` |
| `IconSupport.vue` | `/components/icons/` |
| `IconTooling.vue` | `/components/icons/` |

---

## 路由結構

```
/                     → homeView (公開)
├── /lobby            → lobby (需登入)
├── /privilege        → privilege (需管理員)
├── /conference       → conference (需管理員)
├── /board            → board (需管理員)
│   ├── /demo         → demo (公開)
│   └── /preview      → preview (需管理員)
├── /rule             → rule (需管理員)
│   ├── /:doc_name    → rule (動態路由)
│   └── /demo         → ruleDemo (公開)
├── /log              → log (需管理員)
└── /failed_login     → failedLogin (公開)
```

---

## 狀態管理

### 策略
此專案**未使用**專門的狀態管理庫（如 Vuex/Pinia），而是採用：
- **Props 傳遞**: 父子元件間資料傳遞
- **Component Data**: 元件內部狀態
- **Event Emit**: 子傳父事件

### 主要狀態
| 狀態 | 位置 | 說明 |
|------|------|------|
| `info` | lobby.vue | 使用者資訊 |
| `eventApiUrl` | lobby.vue → calendar.vue | API URL 函式 |

---

## 外部依賴 (CDN)

| 庫 | 版本 | 用途 |
|---|------|------|
| FullCalendar | 6.1.11 | 日曆顯示 |
| Axios | 1.6.7 | HTTP 請求 |
| DOMPurify | 2.4.0 | XSS 防護 |
| SweetAlert2 | 11.7.3 | 彈窗對話框 |

---

## 元件通訊模式

```
┌─────────────────────────────────────────┐
│                  App.vue                 │
└─────────────────┬───────────────────────┘
                  │
         ┌────────┴────────┐
         ▼                 ▼
   ┌──────────┐      ┌──────────┐
   │  lobby   │      │conference│
   └────┬─────┘      └────┬─────┘
        │                 │
   ┌────┴────┐       ┌────┴────┐
   ▼         ▼       ▼         ▼
┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐
│header│ │calendar│ │header│ │calendar│
└──────┘ └───┬───┘ └──────┘ └───┬───┘
             │                   │
        ┌────┴────┐         ┌────┴────┐
        ▼         ▼         ▼         ▼
   ┌─────────┐            ┌─────────┐
   │application│          │application│
   └─────────┘            └─────────┘
```

---

## 可重用元件

| 元件 | 重用次數 | 使用場景 |
|------|----------|----------|
| `header.vue` | 多處 | 所有需要標頭的頁面 |
| `calendar.vue` | 2+ | lobby, conference |
| `application.vue` | 2+ | lobby, conference |
